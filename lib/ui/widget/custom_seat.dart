import 'package:flutter/material.dart';

class CustomSeat extends StatelessWidget {
  const CustomSeat({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}